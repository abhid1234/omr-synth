# Runnable local synthesis/data/eval stack (versions verified in the provided .venv).
music21==10.5.0
verovio==6.2.1
CairoSVG==2.9.0
Pillow==12.3.0
numpy==2.5.1

# Deferred training environment only; intentionally not installed tonight:
# torch>=2.5,<3
# torchvision>=0.20,<1
